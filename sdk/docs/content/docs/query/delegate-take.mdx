---
title: "delegate-take"
description: "A hotkey's delegate take (emission fraction it keeps) with the allowed min/max."
---

{/* GENERATED by scripts/generate.py from the SDK registries — do not edit. */}

`take`, `min`, and `max` are fractions in 0..1; `take_u16` is the raw
on-chain u16 value.

**Category:** Delegation

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `hotkey_ss58` | string | Delegate hotkey whose take to read. |

## CLI

```bash
subtensor query delegate-take --hotkey <ss58|name> --json
```

## Python

```python
import subtensor as sub

async with sub.Client("finney") as client:
    result = await client.read("delegate_take", hotkey_ss58="5F...")
```
